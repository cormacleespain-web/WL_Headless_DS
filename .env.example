# OpenAI: key is used server-side only (proxy avoids CORS). Use OPENAI_API_KEY or VITE_OPENAI_API_KEY.
# Used for: theme generation (GPT) and voice transcription (Whisper). Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=
# VITE_OPENAI_API_KEY=your-key-here

# Optional: model (default is gpt-4o-mini)
# VITE_OPENAI_MODEL=gpt-4o-mini
